kabanicu
    foreach
    	vrijeme.kisa($rain)
    	vrijeme.vjetar($wind)
    assert
    	vrijeme.ponijeti(kabanicu)
kisobran
    foreach
    	vrijeme.kisa($rain)
    	vrijeme.vjetar(False)
    assert
    	vrijeme.ponijeti(kisobran)
nista
    foreach
    	vrijeme.kisa(False)
    	vrijeme.vjetar(False)
    assert
    	vrijeme.ponijeti(nista)
