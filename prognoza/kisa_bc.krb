kabanicu
    use ponijeti(kabanicu)
    when
        vrijeme.kisa(True)
        vrijeme.vjetar(True)
kisobran
    use ponijeti(kisobran)
    when
        vrijeme.kisa(True)
        vrijeme.vjetar(False)
nista
    use ponijeti(nista)
    when
        vrijeme.kisa(False)
        vrijeme.vjetar(False)
vjetrovka
    use ponijeti(vjetrovku)
    when
        vrijeme.kisa(False)
        vrijeme.vjetar(True)
